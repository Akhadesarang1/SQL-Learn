learned about check constraints and also,
created insertfalse.sql file and got this output:
Check constraint 'yearly_salary_positive' is violated.
learned what is unique values and identifiers 
settled unique ids and primery key and had grasp of what is primay key
worked with incrementing id and primary keys 
learned to drop all tables and recreated them with incrementing id , primary key and all required constraints
had a idea to differentiate between
(id SERIAL PRIMARY KEY,-- postgres
id INT PRIMARY KEY AUTO_Increment, -- mysql)