1. Solved a practice solution where i did -
created new DB , added table for product, chose appropriate column name with data types, inserted dummy data into created tables, updated tables and added sensible constraints also added primary key 
2. did both for mysql as well as postgres
3. Had a look into Data Manipulation - CRUD operations
4. Written insert queries to insert single and multiple data and added intentional false data so can work with update keyword
5. Written Update queries to change intentional false data
6. used DELETE keyword so i can remove unwanted rows
7. written multiple select queries where i have performed data expressions , gave alias to column names
8. Had a grasp over what is filtering with WHERE 
9. handled queries for equality and inequality check, used > , <, >=, <= , also used AND OR 
10. had a knowledge of LIMIT,DISTINCT, ASCE, DESC and also tried and passed many queries
11. then written subqueries, orderlimit filter , filter text queries 
did all with practical knowledgge