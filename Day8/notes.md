1. Joining Data & Introducing INNER JOIN  
   - Learned what JOINs are and why they are used.  
   - Introduced to INNER JOIN to combine data from related tables.

2. Using INNER JOIN  
   - Practiced writing INNER JOIN queries.  
   - Learned how to fetch meaningful combined data from multiple tables.

3. Combining Multiple JOINs  
   - Learned how to join more than two tables in a single query.  
   - Understood how table relationships work together in complex queries.

4. Data Joining & Filtering  
   - Combined JOINs with WHERE conditions.  
   - Learned how to filter joined data for precise results.

5. Introducing LEFT JOIN  
   - Learned what LEFT JOIN is and how it differs from INNER JOIN.  
   - Understood that LEFT JOIN returns all records from the left table even if no match exists.

6. Using LEFT JOIN  
   - Practiced writing LEFT JOIN queries.  
   - Learned how NULL values appear when there is no matching record in the right table.

7. Combining Multiple LEFT JOINs  
   - Learned how to use multiple LEFT JOINs in a single query.  
   - Understood how to fetch data from several related tables while preserving left table records.

8. What About RIGHT JOIN?  
   - Learned the concept of RIGHT JOIN.  
   - Understood that RIGHT JOIN works opposite to LEFT JOIN and why LEFT JOIN is usually preferred.

9. Example Time & INNER JOIN vs LEFT JOIN  
   - Compared INNER JOIN and LEFT JOIN using real examples.  
   - Learned when to use INNER JOIN vs LEFT JOIN based on data requirements.

10. Introducing CROSS JOIN  
    - Learned what CROSS JOIN is.  
    - Understood Cartesian product and how rows multiply across tables.

11. UNION & Why It's Different  
    - Learned what UNION is and how it combines result sets vertically.  
    - Understood why UNION is not a JOIN and how it differs conceptually.