1. Created a DB gym and table memberships and inserted data in that with queries for next steps
2. Used Mathematical Functions - CEIL(), FLOOR(), ROUND(), TRUNC()/TRUNCATE()
3. Used String Functions - CONCAT(), LOWER(), LENGTH(), TRIM(), ||
4. Used DATE/TIME Functions - EXRTACT(), ISODOW,  CONVERT, CAST()
5. Also worked with WEEKDAY()